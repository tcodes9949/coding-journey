if (temp > 25) {                        // is temp greater than 25?
  console.log("It's hot")               // prints "It's hot"
} else if (temp >= 15) {                // is temp 15 or more?
  console.log("It's nice")              // prints "It's nice"
} else {                                // nothing above matched
  console.log("It's cold")              // prints "It's cold"
}
